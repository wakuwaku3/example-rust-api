# example-rust-api

## required

- rust

```sh
curl https://sh.rustup.rs -sSf | sh
```

- for debug

```sh
sudo apt search lldb -y
```

## command

- build  
  `cargo build`
- run  
  `cargo run`
- check  
  `cargo check`
- release  
  `cargo build --release`
